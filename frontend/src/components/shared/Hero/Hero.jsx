import React from 'react';
import { Container } from 'react-bootstrap';
import { HeroStyle } from './HeroStyle';

const Hero = () => {
    return (
        <Container fluid style={ HeroStyle.jumbotron }>
            
        </Container>
    )
}

export default Hero
